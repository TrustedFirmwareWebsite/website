---
const { style, text_content } = Astro.props;
import * as marked from "marked";
marked.setOptions({
  renderer: new marked.Renderer(),
});
const markdown = marked.parse(text_content.text);
---

<div class:list={[style, "prose-lg md:prose-xl px-4"]}>
  <p set:html={markdown} />
</div>
